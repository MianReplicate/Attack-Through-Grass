{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "icon": "${logo_file}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "MianReplicate"
  ],
  "contact": {
    "repo": "${repository_url}",
    "issues": "${issue_tracker_url}"
  },
  "license": "${mod_license}",
  "environment": "*",
  "entrypoints": {
    "client": [
      "mc.mian.attackthroughgrass.fabric.ATGFabricClient"
    ],
    "main": [
      "mc.mian.attackthroughgrass.fabric.ATGFabric"
    ]
  },
  "mixins": [
    "${mod_id}-common.mixins.json"
  ],
  "depends": {
    "fabric-api": ">=${fabric_api_version}",
    "minecraft": ">=${minecraft_version}"
  }
}
